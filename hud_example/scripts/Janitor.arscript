extends AROverlap


var dir:Vector2 = Vector2(0, 0)
var is_live = false
var speed:float = 2.0

func move(swipe):
	dir = swipe
	api("out", dir)
	is_live = true
	
func update_process(delta: float) -> void:
	if  is_live:
		self.top = self.top - dir.x*speed
		self.left = self.left + dir.y*speed