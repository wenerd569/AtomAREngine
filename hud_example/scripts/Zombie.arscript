extends ARFillRect

var car


func start():
	car = get_ar_element("Main/Car")
	api("out", car)

func update_process(delta: float) -> void:
	var dir:Vector2 = car. get_global_position( ) - self.get_global_position( )
	dir = dir.normalized( )
	
	self.top += dir.y+car.speed
	self.left += dir.x